from setuptools import setup

setup(
    name="Bucky",
    version="0.0",
    install_requires=[
        "papersize",
        "MySQLdb"  #  Discuss changing to something easier to install??
    ],
    # ...
)
